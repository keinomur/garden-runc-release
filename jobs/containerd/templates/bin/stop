#!/usr/bin/env bash
set -eu

# shellcheck disable=SC1091
source /var/vcap/jobs/containerd/bin/envs

if [ -n "${IS_RUNNING_SYSTEMD}" ]; then
  /bin/systemctl stop containerd.service
else
  /var/vcap/jobs/containerd/bin/containerd_stop
fi
